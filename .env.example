# ============================================
# Dashboard UI - Environment Configuration
# ============================================
#
# Copy this file to .env and configure for your environment:
#   cp .env.example .env
#
# After making changes, restart the development server.

# ============================================
# API Configuration
# ============================================

# Dashboard API Base URL (HTTP/HTTPS)
# This is the REST API endpoint for fetching dashboard data
#
# Local Development: http://localhost:3001
# Production: https://api.yourdomain.com
# Docker: http://dashboard-api:3001
VITE_API_URL=http://localhost:3001

# Dashboard API WebSocket URL (WS/WSS)
# This is the WebSocket endpoint for real-time updates
#
# Local Development: ws://localhost:3001
# Production (SSL): wss://api.yourdomain.com
# Docker: ws://dashboard-api:3001
VITE_WS_URL=ws://localhost:3001

# ============================================
# Development Configuration
# ============================================

# Node Environment
# Options: 'development' | 'production'
#
# Development: Enables debug logs, verbose errors
# Production: Optimized builds, minimal logging
VITE_NODE_ENV=development

# ============================================
# Optional Configuration
# ============================================

# API Timeout (milliseconds)
# Default: 10000 (10 seconds)
# Uncomment to override:
# VITE_API_TIMEOUT=10000

# WebSocket Reconnect Interval (milliseconds)
# Default: 5000 (5 seconds)
# Uncomment to override:
# VITE_WS_RECONNECT_INTERVAL=5000

# Data Polling Interval (milliseconds)
# Fallback polling when WebSocket is disconnected
# Default: 30000 (30 seconds)
# Uncomment to override:
# VITE_POLLING_INTERVAL=30000

# Cache TTL (milliseconds)
# How long to cache data in localStorage
# Default: 60000 (1 minute)
# Uncomment to override:
# VITE_CACHE_TTL=60000

# ============================================
# Deployment Examples
# ============================================

# --- Local Development ---
# VITE_API_URL=http://localhost:3001
# VITE_WS_URL=ws://localhost:3001
# VITE_NODE_ENV=development

# --- Production (Same Host) ---
# VITE_API_URL=https://yourdomain.com/api
# VITE_WS_URL=wss://yourdomain.com/api
# VITE_NODE_ENV=production

# --- Production (Separate API Domain) ---
# VITE_API_URL=https://api.yourdomain.com
# VITE_WS_URL=wss://api.yourdomain.com
# VITE_NODE_ENV=production

# --- Docker Compose ---
# VITE_API_URL=http://dashboard-api:3001
# VITE_WS_URL=ws://dashboard-api:3001
# VITE_NODE_ENV=production

# --- LAN Network (Raspberry Pi, etc.) ---
# VITE_API_URL=http://192.168.1.100:3001
# VITE_WS_URL=ws://192.168.1.100:3001
# VITE_NODE_ENV=production

# ============================================
# Notes
# ============================================
#
# 1. All VITE_* variables are embedded at build time
#    - Changes require rebuilding: npm run build
#    - In development, restart dev server: npm run dev
#
# 2. WebSocket URLs must match protocol:
#    - HTTP API → WS WebSocket
#    - HTTPS API → WSS WebSocket (secure)
#
# 3. CORS must be configured on dashboard-api backend
#    to allow requests from this frontend
#
# 4. For production deployments with HTTPS:
#    - Use wss:// for WebSocket URL
#    - Ensure SSL certificates are valid
#    - Configure reverse proxy (nginx, caddy, etc.)
#
# 5. Never commit .env file to version control
#    - Add .env to .gitignore
#    - Only commit .env.example
